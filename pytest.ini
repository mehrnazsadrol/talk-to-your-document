[pytest]
pythonpath = .
testpaths = tests
addopts = -ra --strict-markers --cov=app --cov=eval --cov-report=term-missing --cov-fail-under=80
markers =
    slow: tests that load real models or otherwise take >1s; excluded by default in CI fast lane
